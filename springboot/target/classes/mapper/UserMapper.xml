<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UserMapper">

    <select id="selectAll" resultType="com.example.entity.User">
        select * from `user`
        <where>
            <if test="name != null">name like concat('%',#{name},'%')</if>
        </where>
        order by id desc
    </select>


    <delete id="deleteById">
        delete from `user` where id = #{id}

    </delete>

    <insert id="insert" parameterType="com.example.entity.User">
        insert into `user` (username,password,name,role,avatar,account)
        values (#{username},#{password},#{name},#{role},#{avatar},#{account})
    </insert>

    <select id="selectByUsername" resultType="com.example.entity.User">
        select * from `user` where username = #{username}
    </select>

    <select id="selectById" resultType="com.example.entity.User">
        select * from `user` where id = #{id}
    </select>

    <update id="updateById">
        update `user` set username = #{username}, password = #{password}, name = #{name},
                          role = #{role}, avatar = #{avatar}, account = #{account}
        where id = #{id}
    </update>
</mapper>